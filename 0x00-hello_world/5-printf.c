#include <stdio.h>

/**
 * main - entry point to the program that print string using prints function.
 *
 * Return: integer value was return
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
